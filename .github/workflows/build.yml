name: Build and Test

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
    
    - name: Install backend dependencies
      run: |
        cd thob
        npm install
    
    - name: Install frontend dependencies
      run: |
        cd thob-cilent/thob-cilent
        npm install
    
    - name: Build backend
      run: |
        cd thob
        npm run build
    
    - name: Build frontend
      run: |
        cd thob-cilent/thob-cilent
        npm run build
